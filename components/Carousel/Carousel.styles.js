import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    marginTop: 20,
    flexDirection: "row",
    flexWrap: "nowrap",
    overflow: "scroll",
    gap: 10,
  },
});

export default styles;
